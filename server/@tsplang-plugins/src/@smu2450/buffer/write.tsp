require("../buffer")

buffer.write = {
    --[[
        TODO

        @param {bufferVar} bufferVar The target buffer.
        @param {(
        buffer.UNIT_AMP
        | buffer.UNIT_VOLT
        | buffer.UNIT_KELVIN
        | buffer.UNIT_OHM)} units The units of the buffer (TODO wording).
    ]]
    format = function(bufferVar, units, displayDigits, extraUnits, extraDigits)
    end
}
