
--[[[
    Iterate over index-value pairs of the given table.

    Iteration stops when the first nil value is reached.

    @param {table} t The target table.

    @returns {function, table, number} A list containing an
        iterator function, a list of values to iterate over (sorted by index),
        and the starting index.
]]
function ipairs(t)
end
