
--[[[
    Checks whether the two arguments are equal without invoking their
    metamethods.

    @param {any} x Some object to compare against object y.
    @param {any} y Some object to compare against object x.

    @returns {boolean} true if x equals y and false otherwise.
]]
function rawequals(x, y)
end
