
--[[[
    Converts the given value to a string.

    If the given value has a "__tostring" field, then the result of calling
    `e.__tostring(e)` is returned.

    @param {any} value The conversion target.

    @returns {string} The given value as a string.
]]
function tostring(value)
end
