require("../string")

--[[[
    Concatenates zero or more character codes into a string.

    @param {number|nil} [charCode=nil] A number from 0-127, inclusive.

    @returns {string} A string whose length is equal to the number of
        arguments given.
]]
string.char = function(charCode, ...)
end
