require("../string")

--[[[
    Returns a string that is the concatenation of the given string repeated a
    number of times.

    @param {string} str The string to repeat.
    @param {number} repeats The number of times to repeat the given string.

    @returns {string} The resulting string.
]]
string.rep = function(str, repeats)
end
