require("../coroutine")

--[[[
    Creates a new coroutine whose body is the given function.

    @param {function} f The body of the coroutine.

    @returns {thread} A new thread object.
]]
coroutine.create = function(f)
end
