require("../table")

--[[[
    Updates the size of the table.

    If the table has a field "n" with a numeric value, then that value is
    updated to the specified size. Otherwise, an internal state is updated.

    @param {table} table
    @param {number} size
]]
table.setn = function(table, size)
end
