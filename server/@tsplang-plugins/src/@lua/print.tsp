
--[[[
    Generate a response message.

    The "tostring" function is called on all arguments before separating each
    with a tab character.

    @param {any} arg An object or value to print.
]]
function print(arg, ...)
end
