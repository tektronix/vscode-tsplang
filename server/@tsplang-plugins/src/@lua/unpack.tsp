
--[[[
    Returns all indexed elements from the given list.

    Equivalent to
    ```lua
    return list[1], list[2], ..., list[n]
    ```
    where _n_ is the size of the list as returned by `table.getn`.

    @param {table} list The table whose indices should be unpacked.

    @returns {any} The values unpacked from the given list.
]]
function unpack(list)
end
