--[[
    Opens the named file and executes its contents as a Lua chunk.

    @param {string} filename The path to the target file.

    @returns {*} The value returned by the chunk.
]]
function dofile(filename)
end
