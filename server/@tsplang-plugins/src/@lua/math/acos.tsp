require("../math")

--[[[
    Calculate the arc cosine, in radians, of the given value in the interval
    [-1, +1]. An error is logged if the given value is outside this interval.

    @param {number} x

    @returns {number}
]]
math.acos = function(x)
end
