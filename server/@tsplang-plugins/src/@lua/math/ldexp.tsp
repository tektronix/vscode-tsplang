require("../math")

--[[[
    Calculates the result of the expression
    ```
    x * 2^exp
    ```

    @param {number} x
    @param {number} exponent

    @returns {number}
]]
math.ldexp = function(x, exponent)
end
